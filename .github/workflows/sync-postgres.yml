name: Sync PostgreSQL Official Image to Aliyun ACR

on:
  workflow_dispatch:

jobs:
  sync-postgres:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to Aliyun ACR
        uses: docker/login-action@v4
        with:
          registry: crpi-cir91a122bvjkhqh.cn-shanghai.personal.cr.aliyuncs.com
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}

      - name: Pull, tag and push PostgreSQL images (latest & alpine)
        run: |
          REGISTRY="crpi-cir91a122bvjkhqh.cn-shanghai.personal.cr.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}"
          
          # 同步 latest 版本
          docker pull postgres:latest
          docker tag postgres:latest $REGISTRY/postgres:latest
          docker push $REGISTRY/postgres:latest
          
          # 同步 alpine 版本
          docker pull postgres:alpine
          docker tag postgres:alpine $REGISTRY/postgres:alpine
          docker push $REGISTRY/postgres:alpine
